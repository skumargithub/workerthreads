package com.kumar;

public interface Constants {
	int SERVER_PORT = 8000;			// Run the server on this port
    int TOP_N = 10;					// We want the results for the words with the Top N counts
    int THREAD_POOL_SIZE = 2;		// Size of the thread pool
}
